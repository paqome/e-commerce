{{ form_start(form, {'attr': {'class': 'row g-3'}}) }}
{{ form_widget(form, {'attr': {'class': 'col-md-6 form-control'}}) }}
<div class="col-12">
	<button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
